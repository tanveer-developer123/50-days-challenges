| Join Type      | Kya deta hai                              |
| -------------- | ----------------------------------------- |
| **INNER JOIN** | Sirf matching rows                        |
| **LEFT JOIN**  | Left table ke sabhi rows + matching right |
| **RIGHT JOIN** | Right table ke sabhi rows + matching left |
| **FULL JOIN**  | Dono ke sabhi rows (match ho ya na ho)    |
| **CROSS JOIN** | Har row × har row                         |
| **SELF JOIN**  | Table ko khud ke saath join               |



Perfect! 😎 Tumne joins ke sare concepts cover kar liye, to ab mai ek detailed PDF-ready content bana deta hoon jisme:

    Har join ka theory

    Syntax aur query

    Example tables aur expected output

    Use cases aur explanation

Mai tumhare liye Markdown format me ready kar deta ho, phir ise PDF me convert kar sakte ho.
SQLite Joins - Complete Guide
1️⃣ INNER JOIN

    Definition: Sirf un rows ko return karta hai jinke values dono tables me match karte ho.

    Syntax:

SELECT columns
FROM table1
INNER JOIN table2
ON table1.column = table2.column;

    Example:

SELECT users.name, posts.title
FROM users
INNER JOIN posts
ON users.id = posts.user_id;

    Explanation: Sirf un users ke posts dikhaye jate hain jinke posts exist karte hain.

2️⃣ LEFT JOIN

    Definition: Left table ke saare rows dikhaye aur match na hone par NULL fill kare.

    Syntax:

SELECT columns
FROM table1
LEFT JOIN table2
ON table1.column = table2.column;

    Example:

SELECT users.name, posts.title
FROM users
LEFT JOIN posts
ON users.id = posts.user_id;

    Explanation: Sab users show honge, chahe posts ho ya na ho.

3️⃣ RIGHT JOIN

    Definition: Right table ke saare rows dikhaye aur match na hone par NULL fill kare.

    Note: SQLite me direct RIGHT JOIN nahi hota, LEFT JOIN ke saath table order swap karke kar sakte hain.

    Example:

SELECT users.name, posts.title
FROM users
RIGHT JOIN posts
ON users.id = posts.user_id;

    Explanation: Sab posts show honge, chahe unka user exist na kare.

4️⃣ FULL OUTER JOIN

    Definition: Dono tables ke saare rows, match na hone par NULL fill kare.

    SQLite workaround: UNION of LEFT JOIN and RIGHT JOIN

SELECT users.name, posts.title
FROM users
LEFT JOIN posts
ON users.id = posts.user_id
UNION
SELECT users.name, posts.title
FROM users
RIGHT JOIN posts
ON users.id = posts.user_id;

5️⃣ CROSS JOIN

    Definition: Cartesian product, sab combinations.

    Syntax:

SELECT columns
FROM table1
CROSS JOIN table2;

    Example:

SELECT users.name, posts.title
FROM users
CROSS JOIN posts;

    Explanation: Har user ke saath har post dikhaye jaayenge.

6️⃣ SELF JOIN

    Definition: Table ko apne aap se join karna. Hierarchies ke liye.

    Syntax:

SELECT e.name AS employee, m.name AS manager
FROM employees e
LEFT JOIN employees m
ON e.manager_id = m.id;

    Example Output:
    | Employee | Manager |
    |----------|---------|
    | Ali | NULL |
    | Ahmed | Ali |
    | Sara | Ali |
    | Bilal | Ahmed |
    | Ayesha | Ahmed |
    | Usman | Sara |

    Explanation: Employee aur manager ke names ek hi table se nikale gaye.

7️⃣ Use Cases Summary
Join Type	Use Case Example
INNER JOIN	Users with existing posts
LEFT JOIN	All users + their posts (null if none)
RIGHT JOIN	All posts + their users (null if no user)
FULL JOIN	Complete list from both tables
CROSS JOIN	Combinations, cartesian products
SELF JOIN	Hierarchy / employee-manager / tree structures

Agar chaho mai is Markdown ko directly PDF me convert kar ke ready file bana dun, taake tum download karke use kar sako?